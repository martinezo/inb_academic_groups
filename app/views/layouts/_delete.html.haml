.modal-dialog.modal-dialog-centered{:role => "document"}
  .modal-content
    .modal-header
      %h5.modal-title= title
      %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
        %span{"aria-hidden" => "true"} ×
    .modal-body
      .alert.alert-danger
        = t('notices.delete_this_record?')
    .modal-footer
      = link_to_fa_button ({url: record, method: :delete, icon: 'fas fa-exclamation-triangle', text: t('commands.delete'), html_class: 'btn btn-danger'})

-# .modal-dialog.modal-md
    .modal-content
      .modal-header
        = button_tag '', data: { dismiss: 'modal'}, class: 'close fa fa-close'
        %h4.modal-title= title
      .modal-body
        .alert.alert-danger
          = t('notices.delete_this_record?')
          .text-right
